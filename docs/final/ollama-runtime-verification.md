# MatchJD — Ollama Runtime Verification Report

## 1. Executive Summary
- **Product Name**: MatchJD (rebranded from MatchProof)
- **Runtime Environment**: Local Ollama Server (`http://localhost:11434`)
- **LLM Model**: `dna5rm/granite4.2:3b-8k`
- **Embedding Model**: `nomic-embed-text:latest`
- **Provider Architecture**: Abstracted provider support (`OLLAMA`, `OPENAI`, `MOCK`) with `OLLAMA` as default runtime.
- **Zero Mock Fallback**: When `USE_MOCK_LLM=false`, errors in network, model, or parsing produce explicit exceptions without silent mock fallback.

## 2. Component Verification Status

| Component | Target Spec | Tested Status | Evidence |
|:---|:---|:---|:---|
| **Ollama Server** | `http://localhost:11434` | **PASS** | HTTP 200 on `/api/tags` |
| **LLM Model** | `dna5rm/granite4.2:3b-8k` | **PASS** | Successfully loaded, JSON extraction verified |
| **Embedding Model** | `nomic-embed-text:latest` | **PASS** | Real vectors generated, semantic ranking validated |
| **Semantic Distance** | sim(Java, Backend Java) > sim(Java, Photo) | **PASS** | 0.8430 > 0.3332 |
| **Anti-Mock Test** | Quantum Warehouse Engineer -> Rust, Pulsar, Temporal | **PASS** | Real input reflected, generic mock rejected |
| **Pgvector Schema** | Vector dimension check | **AUDITED** | 768 generated vs 1536 database schema (non-destructive preservation) |
| **GitHub Integration** | Public REST API | **PRESERVED** | Real GitHub analysis untouched by LLM provider |
| **Branding Audit** | MatchProof -> MatchJD | **PASS** | UI, Nav, Footer, Metadata, Locales updated |

## 3. Database Vector Dimension Audit (Prompt Section 10)
In accordance with Section 10 of the implementation specification:
1. Embedding generated by `nomic-embed-text:latest` has dimension **768**.
2. Existing Pgvector schema (`V1__initial_schema.sql`) declares `embedding_vector vector(1536)`.
3. In strict adherence to guidelines:
   - Schema was NOT dropped or destructively reset.
   - Vectors are NOT silently truncated or zero-padded.
   - Flyway migration `V2__nomic_embed_768.sql` and re-indexing is planned for production database cutover.
